## Quickstart for deploying RDS database to then push data using Terraform and Python
Configured for OIDC authorization through AWS role and attached policy
Environment Variables needed: TF_VAR_db_password
